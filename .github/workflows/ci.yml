name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv sync --locked
      - run: uv run ruff check .
      - run: uv run ruff format --check .

  test:
    runs-on: ubuntu-latest
    env:
      # S3Mock (adobe/s3mock) local como R2 para los tests de integración S3
      # — MinIO ya no se puede tirar anónimo (quay.io/Docker Hub dan 401/
      # pull denied) y el gofakes3 empaquetado en johannesboyne/gofakes3 es
      # un build viejo con bugs de protocolo (cierra la conexión en PUT,
      # HeadObject sin ContentLength).
      R2_ENDPOINT: http://localhost:9090
      R2_BUCKET: nexrad-test
      R2_ACCESS_KEY_ID: minioadmin
      R2_SECRET_ACCESS_KEY: minioadmin
      # Postgres efímero del job (servicio de abajo) para los tests de
      # integración — a diferencia del D1 de test que reemplaza, no hace
      # falta ningún secret de cuenta: el propio job levanta la base.
      PG_HOST: localhost
      PG_PORT: "5432"
      PG_DB: nexrad_test
      PG_USER: nexrad
      PG_PASSWORD: nexrad
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: nexrad_test
          POSTGRES_USER: nexrad
          POSTGRES_PASSWORD: nexrad
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Arrancar S3 Storage Mock
        run: |
          docker run -d --name s3mock -p 9090:9090 \
            -e COM_ADOBE_TESTING_S3MOCK_STORE_INITIAL_BUCKETS=nexrad-test \
            adobe/s3mock:latest
          ready=0
          for i in $(seq 1 30); do
            curl -sf http://localhost:9090/favicon.ico && { ready=1; break; }
            sleep 1
          done
          if [ "$ready" -ne 1 ]; then
            echo "S3 mock no respondió a tiempo" >&2
            docker logs s3mock
            exit 1
          fi
      - run: uv sync --locked
      - name: Aplicar migraciones Postgres
        run: uv run python db/apply_pg_migrations.py "host=localhost port=5432 dbname=nexrad_test user=nexrad password=nexrad"
      - run: uv run pytest

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv build

  # api/ es un subproyecto Python aparte (deps propias, sin rasterio/eccodes/h5py).
  api-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: api
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv sync --locked
      - run: uv run ruff check .
      - run: uv run ruff format --check .
      - run: uv run pytest

  # Imagen única del pipeline (poller + procesador) a ghcr.io, solo en main.
  image:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [lint, test]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:${{ github.sha }}
      # Smoke test DENTRO de la imagen: los tests de pytest corren en el venv
      # del runner y no detectan libs de sistema ausentes en la base slim
      # (p.ej. libexpat que rasterio enlaza del sistema).
      - name: Smoke test de la imagen
        run: |
          docker run --rm --entrypoint python \
            ghcr.io/${{ github.repository }}:${{ github.sha }} \
            -c "import rasterio, metpy, ingest.watcher, ingest.poller; print('imports ok')"

  # Imagen de la API de ops (api/), a ghcr.io, solo en main.
  api-image:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [api-test]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: api
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/${{ github.repository }}-api:latest
            ghcr.io/${{ github.repository }}-api:${{ github.sha }}
